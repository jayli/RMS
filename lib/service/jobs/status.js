// vim: set sw=2 ts=2:

/**
 * @fileoverview check status.
 * @author yyfrankyy@gmail.com (Frank Xu)
 */
var rmsUtil = require('../../rmsUtil');

var status = module.exports = rmsUtil.createJob('nas');

status.prototype.run = function(content, next) {
};
