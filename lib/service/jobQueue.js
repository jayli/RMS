// vim: set sw=2 ts=2:

/**
 * @fileoverview job queue for async query.
 * @author yyfrankyy@gmail.com (Frank Xu)
 */

var fs = require('fs');

function Queue(basedir) {
}

Queue.prototype.get = function(id) {
};

Queue.prototype.del = function(id) {
};

Queue.prototype.put = function(obj) {
};
