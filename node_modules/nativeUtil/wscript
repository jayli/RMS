import Options
from os import unlink, symlink, popen
from os.path import exists 

def set_options(opt):
  opt.tool_options('compiler_cxx')

def configure(conf):
  conf.check_tool('compiler_cxx')
  conf.check_tool('node_addon')

def build(bld):
  obj = bld.new_task_gen('cxx', 'shlib', 'node_addon')
  obj.cxxflags = ['-g', '-D_FILE_OFFSET_BITS=64', '-D_LARGEFILE_SOURCE', '-Wall']
  obj.target = 'nativeUtil'
  obj.source = 'nativeUtil.cc'

# copy from here
# https://github.com/waveto/node-compress/blob/master/wscript#L26
def shutdown():
  # HACK to get compress.node out of build directory.
  # better way to do this?
  if Options.commands['clean']:
    if exists('nativeUtil.node'): unlink('nativeUtil.node')
  else:
    if exists('build/default/nativeUtil.node') and not exists('nativeUtil.node'):
      symlink('build/default/nativeUtil.node', 'nativeUtil.node')
